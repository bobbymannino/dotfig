[package]
name = "dotfig"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
strip = true

[lints.clippy]
pedantic = "warn"
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"
todo = "warn"
unimplemented = "warn"
